<img src="">

<h1>Eat-Da-Burger!</h1>

<h2>Burgers to be eaten</h2>

<ul>
    {{#each burgers}} {{#unless devoured}}
    <li>
        {{id}}: {{burger_name}}

        <button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
            Devour!
        </button>
    </li>
    {{/unless}} {{/each}}
</ul>

<h2>Burgers that have been devoured</h2>

<ul>
    {{#each cats}} {{#if devoured}}
    <li>
        {{burger_name}}
    </li>
    {{/if}} {{/each}}
</ul>